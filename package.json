{
  "name": "ghost-treasury",
  "version": "0.1.0",
  "private": true,
  "description": "Private AI-managed DAO treasury on Monad + Unlink",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:demo-apis\"",
    "dev:backend": "cd packages/backend && npm run dev",
    "dev:frontend": "cd packages/frontend && npm run dev",
    "dev:demo-apis": "cd packages/demo-apis && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  }
}
